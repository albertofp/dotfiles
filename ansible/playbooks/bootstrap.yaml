- hosts: localhost
  name: Bootstrap
  roles:
    - name: homebrew
      when: ansible_distribution == "MacOSX"
    - download_packages
    - sync_keys
    - configs
    - aws
    - zsh
    - name: yabai
      when: ansible_distribution == "MacOSX"
